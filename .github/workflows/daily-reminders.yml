name: Daily License Expiry Reminders

on:
  schedule:
    # Run daily at 9:00 AM UTC (5:30 PM Myanmar Time)
    - cron: '0 9 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  send-reminders:
    runs-on: ubuntu-latest
    
    steps:
    - name: Send Daily Expiry Reminders
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}
      run: |
        curl -X POST "https://kioqpivshgtpacwklcho.supabase.co/functions/v1/send-email-notification" \
          -H "Content-Type: application/json" \
          -d '{
            "to": "ayezinhtun9@gmail.com", 
            "subject": "Daily Reminder Test", 
            "html": "<h1>Daily reminders are working!</h1>"
          }'
